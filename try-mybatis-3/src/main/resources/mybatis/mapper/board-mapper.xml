<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">

	<!-- 전체 조회 -->
	<select id="findAll" resultType="dto">
		SELECT
		id, title, writer, pass, contents
		FROM board2
		ORDER BY id
	</select>

	<!-- 상세 조회 -->
	<select id="findById"
		parameterType="int"
		resultType="dto">
		SELECT
		id, title, writer, pass, contents
		FROM board2
		WHERE id = #{id}
	</select>

	<!-- 등록 -->
	<insert id="insert"
		parameterType="dto">
		INSERT INTO board2
		(
		title, writer, pass, contents
		)
		VALUES
		(
		#{title}, #{writer}, #{pass}, #{contents}
		)
	</insert>

	<!-- 수정 -->
	<update id="update"
		parameterType="dto">
		UPDATE board2
		SET 
		name = #{title}, writer = #{writer},
		pass = #{pass}, contents = #{contents}
		WHERE id = #{id}
	</update>

	<!-- 삭제 -->
	<delete id="delete"
		parameterType="int">
		DELETE
		FROM board2
		WHERE id = #{id}
	</delete>

</mapper>